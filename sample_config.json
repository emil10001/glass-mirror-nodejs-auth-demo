/**
* Move this file to config.json, and fill in the values with your own.
* You'll need to register your app with Google, in their developer 
* console, and enable Mirror API access. You'll also need to run this
* from a publicly accesible address.
*/
{
    "CLIENT_ID" : "YOUR CLIENT ID",
    "CLIENT_SECRET" : "YOUR CLIENT SECRET",
    "REDIRECT_URL" : "http://example.com/oauth2callback"
}
